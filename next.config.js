/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['animals-man-bucket.s3.amazonaws.com', 'animals-man-bucket.s3.eu-north-1.amazonaws.com'],
    },
  };
  
  module.exports = nextConfig;
